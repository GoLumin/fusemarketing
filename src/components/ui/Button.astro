---
interface Props {
  variant?: "primary" | "secondary" | "ghost";
  href?: string;
  size?: "sm" | "md" | "lg";
  class?: string;
}

const { variant = "primary", href, size = "md", class: className } = Astro.props;

const sizeClasses = {
  sm: "px-4 py-2 text-sm",
  md: "px-6 py-3 text-base",
  lg: "px-8 py-4 text-lg",
};

const variantClasses = {
  primary:
    "bg-accent text-white font-semibold hover:bg-accent-hover transition-colors",
  secondary:
    "border border-border text-text-secondary hover:text-text-primary hover:border-border-light transition-colors",
  ghost:
    "text-text-tertiary hover:text-text-primary transition-colors underline-offset-4 hover:underline",
};

const base = `inline-flex items-center justify-center cursor-pointer ${sizeClasses[size]} ${variantClasses[variant]} ${className ?? ""}`.trim();

const Tag = href ? "a" : "button";
---

{href ? (
  <a href={href} class={base}>
    <slot />
  </a>
) : (
  <button class={base}>
    <slot />
  </button>
)}
