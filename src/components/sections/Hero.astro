---
import Button from "../ui/Button.astro";
---

<section class="relative pt-32 pb-20 md:pt-44 md:pb-32 overflow-hidden">
  <!-- Background gradient orbs -->
  <div class="absolute top-0 left-1/4 w-96 h-96 bg-accent-indigo/10 rounded-full blur-3xl"></div>
  <div class="absolute bottom-0 right-1/4 w-96 h-96 bg-accent-cyan/10 rounded-full blur-3xl"></div>

  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <div class="max-w-4xl mx-auto">
      <h1 class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold tracking-tight mb-6">
        Stop Wasting Ad Spend.
        <span class="gradient-text block mt-2">Start Feeding Signals.</span>
      </h1>
      <p class="text-lg md:text-xl text-gray-400 max-w-2xl mx-auto mb-10 leading-relaxed">
        GoFuse connects your leads, quotes, and bookings directly to Google Ads — so the algorithm learns what a real customer looks like.
      </p>
      <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
        <Button href="/get-started" size="lg">Get Started</Button>
        <Button href="/features" variant="secondary" size="lg">See Features</Button>
      </div>
    </div>
  </div>
</section>
